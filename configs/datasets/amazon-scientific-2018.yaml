# ============================================================
# Dataset: Amazon Reviews 2018 — Industrial and Scientific
# Source: https://cseweb.ucsd.edu/~jmcauley/datasets/amazon_v2/
# Category: Industrial_and_Scientific
# ============================================================
#
# Preprocessing required before use:
#   Raw Amazon 2018 JSON → RecBole .inter format
#   Required columns: user_id, item_id, timestamp
#   Run: python src/custom_datasets/preprocess_amazon.py \
#            --category Industrial_and_Scientific --year 2018
#
# Expected file: dataset/amazon-scientific-2018/amazon-scientific-2018.inter
# Format (tab-separated):
#   user_id:token  item_id:token  timestamp:float
# ============================================================

dataset: amazon-scientific-2018

# Field mapping (must match .inter file header)
USER_ID_FIELD: user_id
ITEM_ID_FIELD: item_id
TIME_FIELD: timestamp

# Only load interaction data
load_col:
  inter: [user_id, item_id, timestamp]

# 5-core filtering
user_inter_num_interval: '[5, inf)'
item_inter_num_interval: '[5, inf)'
